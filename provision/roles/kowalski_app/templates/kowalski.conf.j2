# Kowalski OS configuration — managed by Ansible (provision/roles/kowalski_app).
# Edit the role vars / pass -e to change these, not this file (it is overwritten).
OLLAMA_HOST={{ kow_ollama_host }}
OLLAMA_MODEL={{ kow_ollama_model }}
KOW_EMBED_MODEL={{ kow_embed_model }}
KOW_VISION={{ kow_vision }}
KOW_ALLOWED_PATHS={{ kow_allowed_paths }}
