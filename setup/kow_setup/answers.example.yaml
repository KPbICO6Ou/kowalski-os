# Example answers for kow-setup --non-interactive --answers <file>
# Copy and edit; the real file may contain tokens — keep it out of git.
ollama:
  mode: remote
  url: "http://10.0.0.5:11434"
  model: "qwen2.5:14b"
stt:
  mode: remote
  url: "http://10.0.0.5:5099"
  token: ""
  language: "ru"
tts:
  mode: skip
# Optional wake activation (push_to_talk | wake_word | both).
# voice:
#   wake_mode: both
#   wake_word: kowalski
# Optional mailbox (mode: imap | mock | skip). Secrets land in the 0600 conf;
# for Gmail/Outlook use an app-password. SMTP host/user default to the IMAP ones.
# mail:
#   mode: imap
#   imap_host: "imap.example.com"
#   imap_port: "993"
#   imap_user: "me@example.com"
#   imap_password: "app-password"
#   imap_ssl: "1"
#   smtp_host: "smtp.example.com"
#   smtp_port: "587"
#   smtp_user: "me@example.com"
#   smtp_tls: "1"
